<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Flare Quotes visual system
        Flat, restrained Windows 11-inspired surfaces. All colors are dynamic so
        MainWindow can switch the existing light/dark preference without replacing
        controls or disturbing workflow state.
    -->
    <SolidColorBrush x:Key="FlareTextBrush" Color="#F5F3EE" />
    <SolidColorBrush x:Key="FlareMutedTextBrush" Color="#A4A8AE" />
    <SolidColorBrush x:Key="FlareAccentBrush" Color="#99CC00" />
    <SolidColorBrush x:Key="FlareAccentTextBrush" Color="#141A00" />
    <SolidColorBrush x:Key="FlarePrimaryGreenBrush" Color="#99CC00" />
    <SolidColorBrush x:Key="FlarePrimaryGreenHoverBrush" Color="#A9DA1C" />
    <SolidColorBrush x:Key="FlarePrimaryGreenPressedBrush" Color="#82AE00" />
    <SolidColorBrush x:Key="FlarePrimaryGreenBorderBrush" Color="#99CC00" />

    <SolidColorBrush x:Key="WindowSurfaceBrush" Color="#0B0D10" />
    <SolidColorBrush x:Key="WindowTitleBarBrush" Color="#090B0E" />
    <SolidColorBrush x:Key="WindowHeaderBrush" Color="#0E1115" />
    <SolidColorBrush x:Key="WindowWorkspaceBrush" Color="#090C10" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="#12161B" />
    <SolidColorBrush x:Key="SurfaceRaisedBrush" Color="#171C22" />
    <SolidColorBrush x:Key="SurfaceSubtleBrush" Color="#0F1318" />
    <SolidColorBrush x:Key="DividerBrush" Color="#242A31" />
    <SolidColorBrush x:Key="ControlBorderBrush" Color="#303841" />
    <SolidColorBrush x:Key="ControlHoverBrush" Color="#20262D" />
    <SolidColorBrush x:Key="ControlPressedBrush" Color="#262D35" />
    <SolidColorBrush x:Key="FocusRingBrush" Color="#789B00" />
    <SolidColorBrush x:Key="SuccessMutedBrush" Color="#B4C58C" />
    <SolidColorBrush x:Key="WarningBrush" Color="#D4A94F" />
    <SolidColorBrush x:Key="DangerBrush" Color="#D1493F" />

    <!-- Device-independent vector iconography. These paths remain crisp at every
         Windows DPI setting and avoid font-glyph fallback/pixelation. -->
    <Geometry x:Key="SunIconGeometry">M8,3 A5,5 0 1 1 7.99,3 M8,0 L8,1.5 M8,14.5 L8,16 M0,8 L1.5,8 M14.5,8 L16,8 M2.35,2.35 L3.4,3.4 M12.6,12.6 L13.65,13.65 M13.65,2.35 L12.6,3.4 M3.4,12.6 L2.35,13.65</Geometry>
    <Geometry x:Key="MoonIconGeometry">M13.8,10.8 C12.7,13.2 10.3,14.7 7.7,14.3 C4.1,13.8 1.6,10.5 2.1,6.9 C2.45,4.45 4.15,2.45 6.45,1.55 C5.65,2.75 5.3,4.25 5.5,5.75 C5.95,9.25 9.05,11.75 12.55,11.3 C12.98,11.24 13.4,11.07 13.8,10.8 Z</Geometry>
    <Geometry x:Key="SettingsIconGeometry">M1,4 L15,4 M5,1 L5,7 M1,12 L15,12 M11,9 L11,15</Geometry>
    <Geometry x:Key="HomeIconGeometry">M1.5,7.5 L8,1.5 L14.5,7.5 M3.5,6.2 L3.5,14.5 L12.5,14.5 L12.5,6.2 M6.5,14.5 L6.5,10 L9.5,10 L9.5,14.5</Geometry>
    <Geometry x:Key="MailIconGeometry">M1.5,3.5 H14.5 V12.5 H1.5 Z M2,4 L8,9 L14,4</Geometry>
    <Geometry x:Key="FolderIconGeometry">M1.5,4 H6 L7.5,5.5 H14.5 V13.5 H1.5 Z</Geometry>
    <Geometry x:Key="ClockIconGeometry">M8,1.5 A6.5,6.5 0 1 1 7.99,1.5 M8,4.2 L8,8 L10.7,9.7</Geometry>
    <Geometry x:Key="PlusIconGeometry">M8,2 L8,14 M2,8 L14,8</Geometry>
    <Geometry x:Key="RecentIconGeometry">M2,5 L2,1 M2,1 L6,1 M2.5,2.5 A6,6 0 1 1 2.5,11.5</Geometry>
    <Geometry x:Key="MinimizeIconGeometry">M2,8 H14</Geometry>
    <Geometry x:Key="MaximizeIconGeometry">M3,3 H13 V13 H3 Z</Geometry>
    <Geometry x:Key="CloseIconGeometry">M3,3 L13,13 M13,3 L3,13</Geometry>
    <Geometry x:Key="CheckIconGeometry">M2.5,8.5 L6.4,12.2 L13.8,4.2</Geometry>

    <!-- Compatibility keys retained for existing views and bindings. -->
    <LinearGradientBrush x:Key="FlareDarkBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#0B0D10" Offset="0" />
        <GradientStop Color="#0B0D10" Offset="1" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="FlareBorderBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#2A3038" Offset="0" />
        <GradientStop Color="#2A3038" Offset="1" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="FlareHeaderBrush" Color="#0E1115" />
    <LinearGradientBrush x:Key="GlassBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#0B0D10" Offset="0" />
        <GradientStop Color="#0B0D10" Offset="1" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="GlassBorderBrush" Color="#2A3038" />
    <SolidColorBrush x:Key="GlassHeaderBrush" Color="#0E1115" />
    <SolidColorBrush x:Key="GlassCardBrush" Color="#12161B" />
    <SolidColorBrush x:Key="GlassCardAltBrush" Color="#0F1318" />
    <SolidColorBrush x:Key="GlassInputBrush" Color="#0D1014" />
    <SolidColorBrush x:Key="FlareCardBrush" Color="#12161B" />
    <SolidColorBrush x:Key="FlareCardAltBrush" Color="#0F1318" />
    <SolidColorBrush x:Key="GlassPopupBrush" Color="#171C22" />
    <SolidColorBrush x:Key="PopupTextBrush" Color="#F5F3EE" />
    <SolidColorBrush x:Key="DropdownPopupBrush" Color="#171C22" />
    <SolidColorBrush x:Key="DropdownInputBrush" Color="#0D1014" />
    <SolidColorBrush x:Key="DropdownOptionBrush" Color="#181D23" />
    <SolidColorBrush x:Key="DropdownOptionHoverBrush" Color="#232A32" />
    <SolidColorBrush x:Key="DropdownOptionBorderBrush" Color="#323A44" />
    <SolidColorBrush x:Key="DropdownTextBrush" Color="#F5F3EE" />
    <SolidColorBrush x:Key="DropdownMutedTextBrush" Color="#A4A8AE" />
    <SolidColorBrush x:Key="FlareInputBrush" Color="#0D1014" />
    <SolidColorBrush x:Key="FlareButtonBrush" Color="#171C22" />
    <SolidColorBrush x:Key="GlassButtonHoverBrush" Color="#20262D" />
    <SolidColorBrush x:Key="FlareButtonBorderBrush" Color="#343C46" />
    <SolidColorBrush x:Key="FlareChipBrush" Color="#181D23" />
    <SolidColorBrush x:Key="FlareChipBorderBrush" Color="#323A44" />

    <Style x:Key="FlareFocusVisual" TargetType="Control">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border Margin="-3"
                            BorderBrush="{DynamicResource FocusRingBrush}"
                            BorderThickness="2"
                            CornerRadius="9"
                            SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Window">
        <Setter Property="Foreground" Value="{DynamicResource FlareTextBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
        <Setter Property="FontSize" Value="13.5" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource FlareTextBrush}" />
        <Setter Property="TextTrimming" Value="None" />
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{DynamicResource FlareTextBrush}" />
    </Style>

    <Style x:Key="MutedText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="Foreground" Value="{DynamicResource FlareMutedTextBrush}" />
    </Style>

    <Style x:Key="EyebrowText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="Foreground" Value="{DynamicResource FlareAccentBrush}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <Style x:Key="SectionTitleText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="GlassCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="14" />
        <Setter Property="Padding" Value="20" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="Card" TargetType="Border" BasedOn="{StaticResource GlassCard}" />

    <Style x:Key="SubtleCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SurfaceSubtleBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="16" />
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Background" Value="{DynamicResource FlareInputBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource FlareTextBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,9" />
        <Setter Property="CaretBrush" Value="{DynamicResource FlareTextBrush}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource FlareAccentBrush}" />
        <Setter Property="SelectionTextBrush" Value="{DynamicResource FlareAccentTextBrush}" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FlareFocusVisual}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="InputBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                      HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource FocusRingBrush}" />
                            <Setter TargetName="InputBorder" Property="BorderThickness" Value="1.5" />
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter Property="Opacity" Value="0.78" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Background" Value="{DynamicResource FlareButtonBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource FlareTextBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource FlareButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FlareFocusVisual}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ControlPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.42" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{DynamicResource FlarePrimaryGreenBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource FlareAccentTextBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource FlarePrimaryGreenBorderBrush}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource FlarePrimaryGreenHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource FlarePrimaryGreenPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="QuietButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource FlareMutedTextBrush}" />
    </Style>

    <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource QuietButton}">
        <Setter Property="Width" Value="40" />
        <Setter Property="Height" Value="40" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="ChipRemoveButton" TargetType="Button" BasedOn="{StaticResource QuietButton}">
        <Setter Property="Width" Value="24" />
        <Setter Property="Height" Value="24" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="CaptionButton" TargetType="Button" BasedOn="{StaticResource QuietButton}">
        <Setter Property="Width" Value="44" />
        <Setter Property="Height" Value="55" />
        <Setter Property="MinHeight" Value="55" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    </Style>

    <Style x:Key="CloseCaptionButton" TargetType="Button" BasedOn="{StaticResource CaptionButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder" Background="Transparent">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource DangerBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.8" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ToggleButton">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Background" Value="{DynamicResource FlareButtonBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource FlareTextBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource FlareButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,7" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FlareFocusVisual}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="ToggleBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ToggleBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ToggleBorder" Property="Background" Value="{DynamicResource SurfaceRaisedBrush}" />
                            <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="{DynamicResource FocusRingBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.42" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ModeToggleStyle" TargetType="ToggleButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
        <Setter Property="Width" Value="40" />
        <Setter Property="Height" Value="40" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Background" Value="{DynamicResource FlareInputBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource FlareTextBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,9" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FlareFocusVisual}" />
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
        <Setter Property="Padding" Value="10,8" />
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource FlareTextBrush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinHeight" Value="28" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FlareFocusVisual}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border x:Name="Box"
                                Width="18"
                                Height="18"
                                CornerRadius="4"
                                Background="{DynamicResource FlareInputBrush}"
                                BorderBrush="{DynamicResource ControlBorderBrush}"
                                BorderThickness="1"
                                VerticalAlignment="Center">
                            <Path x:Name="CheckMark"
                                  Data="M 3 8 L 7 12 L 15 4"
                                  Stroke="{DynamicResource FlareAccentTextBrush}"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter Grid.Column="1"
                                          Margin="9,0,0,0"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{DynamicResource FlareAccentBrush}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource FlareAccentBrush}" />
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource FocusRingBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{DynamicResource DropdownPopupBrush}"
                            BorderBrush="{DynamicResource DropdownOptionBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="6"
                            SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="10" Direction="270" Opacity="0.32" />
                        </Border.Effect>
                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="11,8" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="ItemBorder"
                            Margin="1"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="10">
                        <ContentPresenter ContentSource="Header"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource DropdownOptionHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.42" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{DynamicResource SurfaceRaisedBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource FlareTextBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="9,6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InvisibleScrollRepeatButton" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton"><Border Background="Transparent" /></ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="IntegratedScrollThumb" TargetType="Thumb">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="ThumbBorder"
                            Width="5"
                            Margin="1,2"
                            CornerRadius="3"
                            Background="{DynamicResource FlareMutedTextBrush}"
                            Opacity="0.38" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Opacity" Value="0.76" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Width" Value="8" />
        <Setter Property="MinWidth" Value="8" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent" Width="8" Margin="2,0,0,0">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource InvisibleScrollRepeatButton}" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb><Thumb Style="{StaticResource IntegratedScrollThumb}" /></Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource InvisibleScrollRepeatButton}" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
